{
  "name": "hd-karya-bandung",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --test",
    "dev": "wrangler dev --local",
    "d1:migrate:local": "wrangler d1 migrations apply hd-karya-bandung --local",
    "types": "wrangler types ./src/worker-configuration.d.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.120.0"
  }
}
